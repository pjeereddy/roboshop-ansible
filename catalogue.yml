#component=catalogue
#schema_type=mongodb
#source common.sh
#func_nodejs

- name: RoboShop Project - Frontend
  hosts: all
  become: true
  vars:
    component: catalogue
  tasks:
    ansible.builtin.import_tasks: nodejs.yml







